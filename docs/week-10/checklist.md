# 10주차 과제 진행 체크리스트

> 기준 문서: [`docs/assignments/week-10.md`](../assignments/week-10.md)
>
> Week 10 작업을 시작하거나 완료 여부를 판단할 때 이 문서와 과제 원문을 먼저 읽는다. 근거를
> 확인하지 않은 항목은 완료로 표시하지 않는다.

## 진행 원칙

- [ ] AI가 제안한 병목, workflow, 규칙과 리뷰 결과를 작성자가 로그와 자가 검증으로 확인했는가
- [ ] required 검증, 예산 임계값, E2E 실행 조건, AI 리뷰 배치와 승격할 규칙은 작성자가 직접
      판단했는가
- [ ] 구현 중 판단이 바뀌면 이전 판단을 지우지 않고 바뀐 이유와 근거를 문서에 남겼는가
- [ ] 측정값, CI run, PR, 로그와 스크린샷처럼 다시 확인할 수 있는 증거를 연결했는가

## 1단계 — CI 측정·최적화

### 해야 하는 것

- [ ] 기존 workflow를 먼저 확인하고, 새 workflow를 만들기보다 부족한 단계를 보강했는가
- [ ] `.nvmrc`, `package.json#packageManager`, 실제 `scripts`와 workflow의 명령이 일치하는가
- [ ] 프로젝트에 존재하는 lint, typecheck, test, build를 CI에서 모두 실행하는가
- [ ] `pnpm install --frozen-lockfile`을 사용하고 job에 `timeout-minutes`를 두었는가
- [x] Before의 cold 3회와 warm 3회를 구분해 raw 값, 중앙값과 범위를 남겼는가
- [x] 같은 실행에서 workflow 전체, job과 주요 step 시간을 기록했는가
- [x] 가장 긴 구간을 로그의 시간으로 지목했는가
- [x] Before에서 확인한 병목에 맞는 전략만 적용했는가
- [x] After도 같은 조건으로 cold 3회와 warm 3회를 측정했는가
- [x] Before/After의 검증 항목과 러너·Node 조건이 같은가
- [x] 감소 폭이 측정 범위보다 큰지, 지목한 병목의 감소와 연결되는지 설명했는가
- [x] warm 로그에서 캐시 복원 메시지를 확인하고 캡처했는가
- [x] 유효한 lockfile 변경으로 캐시 miss를 재현하고 hit/miss의 install 시간을 비교했는가
- [x] 캐시 miss 실험이 끝난 뒤 lockfile 변경을 복구했는가

### 하지 말아야 하는 것

- [ ] 시간을 줄이기 위해 필요한 test나 다른 검증을 제거하지 않았는가
- [ ] 측정하지 않은 병목을 추측만으로 최적화하지 않았는가
- [ ] 1단계에서 path filter를 최적화 수단으로 먼저 적용하지 않았는가
- [ ] job 병렬화로 각 job의 install 비용을 중복시키지 않았는가
- [ ] `concurrency`가 다른 브랜치나 `main` 실행까지 취소하도록 만들지 않았는가
- [ ] 캐시 설정의 존재만 보고 hit이라고 기록하지 않았는가

### 현재 확인한 참고 실행 — Week 09 PR #181

이 값은 cold/warm 여부가 확인되지 않아 Before 3회 측정에는 아직 포함하지 않는다.

| 범위                                  |       값 |
| ------------------------------------- | -------: |
| Quality workflow 전체                 | 1분 23초 |
| quality job                           | 1분 18초 |
| Set up job                            |      1초 |
| Checkout                              |      1초 |
| Set up pnpm                           |      3초 |
| Set up Node.js                        |     10초 |
| Install dependencies                  |      2초 |
| Install Playwright Chromium when used |     24초 |
| Run quality checks                    |     33초 |
| Post Set up Node.js                   |      0초 |
| Post Set up pnpm                      |      1초 |
| Post Checkout                         |      0초 |
| Complete job                          |      0초 |

- Chromium 설치는 `pnpm check`에서 사용되지 않으므로 제거 후보지만, 24초가 곧 확정 단축 시간은
  아니다. 같은 조건의 Before/After로 확인한다.
- Quality도 약 197MB의 pnpm dependency cache를 복원했으므로 pnpm cache 기준 warm이다. Next.js
  build cache는 없었다.
- E2E 참고 실행은 workflow 전체 2분 30초, e2e job 2분 26초였다. 브라우저 설치는 47초,
  `Run E2E tests`는 1분 19초였다. 후자는 production build와 Playwright 실행을 모두 포함한다.
  Playwright는 Chromium·WebKit 30개 테스트를 4 workers로 실행해 1.2분에 모두 통과했다. Next.js
  build cache는 없었고, pnpm dependency cache 약 197MB는 key가 일치해 복원됐다. 따라서 pnpm
  cache 기준 warm 실행이지만 모든 캐시가 warm이었다고 표현하지 않는다.

## 2단계 — 조건부 실행

### 해야 하는 것

- [ ] lint, typecheck와 unit test는 모든 PR에서 실행하는가
- [ ] E2E 또는 가장 비싼 검증 하나 이상에 프로젝트 상황에 맞는 실행 조건을 설계했는가
- [ ] 어떤 변경을 스킵하며 그 스킵이 안전한 이유를 적었는가
- [ ] 조건에 걸리는 PR과 걸리지 않는 PR을 각각 만들어 Actions 로그를 남겼는가
- [ ] required check와 조건부 실행이 충돌하지 않아 PR이 머지 가능한가
- [ ] E2E retry 또는 반복 실패 처리 정책을 이유와 함께 1~2줄로 남겼는가

### 하지 말아야 하는 것

- [ ] path filter를 너무 넓거나 좁게 정하고 안전 근거 없이 사용하지 않았는가
- [ ] 조건부로 생성되지 않는 check를 그대로 required로 두지 않았는가
- [ ] label 재부착 시 재실행과 권한을 확인하지 않은 채 label 트리거를 채택하지 않았는가
- [ ] retry로 최초 실패나 실제 결함을 감추지 않았는가
- [ ] 사용할 수 있는지 확인하지 않은 merge queue를 최종 방어 근거로 삼지 않았는가

## 3단계 — 예산 게이트와 결과 가시성

### 해야 하는 것

- [ ] 번들 예산이 7주차 실제 전송 크기와 현재 값의 범위에 근거하는가
- [ ] 임계값과 여유폭을 정한 이유를 문서에 남겼는가
- [ ] 필수 환경 변수의 누락, 잘못된 URL과 비공개 값의 `NEXT_PUBLIC_` 노출을 build 전에 막는가
- [ ] required로 둘 게이트와 제외할 게이트를 비용·변동성·리스크로 구분했는가
- [ ] PR 코멘트나 job summary에서 측정값, 한도와 초과량을 바로 확인할 수 있는가
- [ ] 예산을 일부러 초과한 PR에서 빨간불과 실패 리포트를 남겼는가
- [ ] 수정 후 다시 초록불이 된 증거를 남겼는가
- [ ] 실험 PR을 머지하지 않았고 실험 변경을 최종 코드에서 제거했는가

### 하지 말아야 하는 것

- [ ] 근거 없이 `300KB` 같은 임의의 예산을 정하지 않았는가
- [ ] 전체 페이지 전송량과 JavaScript 번들 크기를 같은 값으로 취급하지 않았는가
- [ ] 실패 원인을 확인하려면 로그를 열어야만 하는 상태로 두지 않았는가
- [ ] 실제 secret 값을 코드, 문서, 로그나 PR에 남기지 않았는가

## 4단계 — AI 코드리뷰

### AI에게 맡길 수 있는 범위

- workflow YAML 초안과 기존 workflow의 누락·위험 검토
- size-limit과 환경 변수 검증 스크립트의 골격
- `dorny/paths-filter`, `concurrency`, job summary 같은 설정 초안
- ESLint custom rule, `no-restricted-syntax` selector와 Danger 스크립트 골격
- 10주간 합의한 팀 컨벤션을 리뷰 프롬프트 형태로 정리하는 일
- 현재 PR diff 셀프 리뷰와 설계 냄새 후보 제시
- CI 로그의 step 시간 정리와 병목 후보 분석
- 회고 초안의 구조·문장·근거 누락 검토

도구는 Codex, Claude Code, Gemini CLI, 구독 챗봇과 무료 티어 등 자유롭게 선택할 수 있다. 특정
유료 API 결제는 요구사항이 아니며, AI 리뷰를 GitHub Actions에 통합하는 것도 선택이다.

### 작성자가 직접 해야 하는 판단

- [ ] 어떤 status check를 branch protection의 required로 둘지 직접 결정했는가
- [ ] 번들 예산 임계값과 여유폭을 실제 측정값을 보고 직접 결정했는가
- [ ] E2E 같은 비싼 검증을 어떤 변경과 시점에 실행할지 직접 결정했는가
- [ ] AI 리뷰를 required가 아닌 advisory로 둘지, 어디에서 실행할지 직접 결정했는가
- [ ] 어떤 반복 지적을 결정적 룰로 승격할지 직접 결정했는가
- [ ] AI가 제시한 리뷰를 잘 잡은 것과 헛소리로 직접 판별했는가
- [ ] 무엇을 AI·사람의 맥락 판단에 남기고 무엇을 기계적 게이트로 내릴지 직접 설명했는가

이 판단들은 AI가 선택지를 정리하고 위험을 설명할 수는 있지만 대신 확정하면 안 된다. 사용자의
판단이 나오기 전에는 체크하지 않는다.

### AI 결과를 채택하기 전에 반드시 검증할 것

- [ ] AI가 지목한 병목이 실제 Actions step 시간에서 가장 긴 구간인지 직접 확인했는가
- [ ] AI가 만든 캐시 설정이 warm 로그에서 실제 hit 되는지 확인했는가
- [ ] AI가 만든 path filter가 필요한 검증을 스킵하지 않는지 실행·스킵 PR로 확인했는가
- [ ] AI가 만든 `concurrency`가 같은 PR의 중복 실행만 취소하고 `main`이나 다른 ref를 건드리지
      않는가
- [ ] AI가 제안한 required check가 조건부 스킵 때문에 영구 대기하지 않는가
- [ ] AI가 만든 예산·환경 변수 게이트가 의도적인 위반을 실제로 빨간불로 막는가
- [ ] AI가 만든 결정적 룰이 위반 코드는 막고 정상 코드는 통과시키는가
- [ ] AI 리뷰 지적을 실제 diff와 팀 규칙에 대조해 채택 또는 기각 근거를 남겼는가
- [ ] AI가 만든 workflow의 권한, trigger, action pin과 secret 접근을 직접 검토했는가

### 해야 하는 것

- [ ] 10주 동안 합의한 프로젝트 규칙을 구체적인 리뷰 기준으로 작성했는가
- [ ] 현재 PR diff를 사용 중인 AI 도구로 리뷰했는가
- [ ] 잘 잡아낸 리뷰 1개와 헛소리한 리뷰 1개를 직접 판별해 제출했는가
- [ ] 헛소리를 줄이기 위해 프롬프트를 어떻게 수정했는지 남겼는가
- [ ] AI 리뷰를 CI에 붙였다면 timeout, max turns, concurrency와 명시적 트리거를 검토했는가

### 하지 말아야 하는 것

- [ ] AI 리뷰를 검증 없이 사실로 채택하지 않았는가
- [ ] 비결정적인 AI 리뷰를 기본적으로 required 게이트로 두지 않았는가
- [ ] 과제 때문에 유료 API 결제가 필수라고 판단하지 않았는가
- [ ] “가독성 좋게” 같은 일반론만 리뷰 기준으로 사용하지 않았는가

### AI 리뷰 프롬프트에 포함할 프로젝트 기준

- [ ] PR에서 `main`에 깨진 코드가 들어가는 것을 막기에 검증이 충분한지 묻는가
- [ ] `concurrency` 그룹이 `main` 실행까지 취소하지 않는지 묻는가
- [ ] 캐시 키가 실제로 hit 될 구조인지 묻는가
- [ ] path filter가 필요한 검증을 스킵하지 않는지 묻는가
- [ ] required check와 조건부 실행이 충돌하지 않는지 묻는가
- [ ] workflow 권한이 최소인지, secrets가 노출될 자리가 없는지 묻는가
- [ ] 무의미한 `any`, 타입 단언과 `eslint-disable`을 확인하는가
- [ ] 파생 값을 effect로 동기화하거나 서버 응답을 로컬 상태에 복사하지 않는지 확인하는가
- [ ] custom hook 분리 기준과 URL 상태 동기화 규칙을 확인하는가
- [ ] FSD import 방향과 slice Public API 우회를 확인하는가

### AI 리뷰를 CI에 통합할 때만 추가할 것

- [ ] `max_turns`와 `timeout_minutes`로 무한 실행과 과금을 막았는가
- [ ] `concurrency`로 같은 PR의 중복 AI 리뷰를 취소하는가
- [ ] `@ai` 코멘트나 label처럼 사람이 명시적으로 부를 때만 실행할지 검토했는가
- [ ] 모든 PR 자동 실행을 골랐다면 비용과 리뷰 소음의 근거를 남겼는가
- [ ] AI 리뷰가 실패하거나 오탐을 내도 결정적 CI 결과를 뒤집지 않는가
- [ ] API key가 fork PR이나 로그에 노출되지 않는가
- [ ] `pull_request_target`이 꼭 필요한지 검토하고 PR 코드를 checkout해 실행하지 않는가

### 제출할 AI 활용 증거

- [ ] 실제 사용한 팀 컨벤션 기반 리뷰 프롬프트가 남아 있는가
- [ ] AI가 잘 잡아낸 리뷰 1개와 채택 근거가 있는가
- [ ] AI가 헛소리한 리뷰 1개와 기각 근거가 있는가
- [ ] 헛소리를 줄이기 위해 바꾼 프롬프트의 전후와 변경 이유가 있는가
- [ ] AI가 제안하고 작성자가 확인·판단한 부분의 역할 구분이 드러나는가
- [ ] CI에 통합했다면 비용·폭주·권한 안전장치와 게이트 배치 판단이 문서에 있는가

## 5단계 — 반복 지적을 결정적 룰로 승격

### 해야 하는 것

- [ ] AI 또는 사람에게 반복적으로 지적받은 실제 출처가 있는 규칙 하나를 골랐는가
- [ ] 문맥 없이 참/거짓을 판별할 수 있는 범위만 기계적 게이트로 내렸는가
- [ ] 위반 코드는 실패하고 정상 코드는 통과하는지 자가 검증했는가
- [ ] 오탐이 있다면 규칙을 좁히고 다시 검증했는가
- [ ] AI·사람에게 남길 판단과 기계로 내릴 판단을 한 문단으로 설명했는가

### 하지 말아야 하는 것

- [ ] 맥락이 필요한 모든 설계 지적을 억지로 ESLint나 grep 규칙으로 만들지 않았는가
- [ ] 이미 존재하는 규칙을 이번 주에 새로 승격한 것으로 기록하지 않았는가
- [ ] AI가 만든 룰을 위반/정상 예제로 검증하지 않은 채 채택하지 않았는가

## 6단계 — 기술 회고와 질문

- [ ] `docs/rfc/week10-retrospective.md`를 작성했는가
- [ ] 기능 목록보다 기술 결정 2~3개, 근거, 결과와 한계를 중심으로 썼는가
- [ ] 숫자, URL, 로그와 CI run으로 주장을 다시 확인할 수 있는가
- [ ] AI·사람에게 남긴 판단과 기계로 내린 규칙이 10주 전체의 결론과 이어지는가
- [ ] 제출 질문 4개에 각각 2~4문장으로 작성자의 판단과 근거를 적었는가

## 공통 보안

- [ ] workflow 또는 job의 기본 권한을 `contents: read` 등 최소 권한으로 명시했는가
- [ ] PR 코멘트 권한은 필요한 job에만 `pull-requests: write`로 추가했는가
- [ ] third-party action을 태그 또는 SHA 중 어디까지 고정할지 판단하고 근거를 남겼는가
- [ ] 불필요한 `pull_request_target`을 사용하지 않았는가
- [ ] `pull_request_target`을 쓴다면 fork PR 코드를 checkout해 실행하지 않는가
- [ ] secrets를 로그에 출력하거나 fork PR에서 접근 가능한 위치에 두지 않았는가

## 선택 또는 과제 밖

- Lighthouse CI는 선택이다. 도입한다면 7주차 측정값을 임계값 근거로 사용하고 변동성에 맞는 실행
  조건을 정한다.
- AI 리뷰의 GitHub Actions 통합은 선택이다. 로컬 도구로 리뷰해도 된다.
- rollback 심화는 선택이며 이번 주 산출물 밖이다.
- Preview/Production URL 확보는 권장되지만 배포 자체는 이번 주 채점 대상이 아니다.

## 단계별 완료조건

### 1단계 완료조건 — CI 측정·최적화

아래 증거를 모두 제출물에서 확인할 수 있어야 완료다.

> After cold 반복값은 3회를 확보했지만, 별도 cache 실험에서는 matrix job 간 key 공유로 일부 job만
> miss가 됐다. 이 한계는 RFC와 개인 조사 기록에 명시한다.

- [x] Before cold 3회와 warm 3회의 raw 값, 중앙값과 범위
- [x] After cold 3회와 warm 3회의 raw 값, 중앙값과 범위
- [x] Actions 로그 시간으로 지목한 병목 1문단
- [x] 캐시 hit 로그와 의도적으로 재현한 miss 로그
- [x] hit/miss의 install 시간 비교
- [x] 선택한 최적화가 병목에 맞는 이유와 다른 전략을 적용하지 않은 이유
- [x] Before/After에서 같은 검증 항목을 유지했다는 대조
- [x] 감소 폭이 측정 흔들림보다 큰지와 병목 감소에 연결되는지에 대한 설명

### 2단계 완료조건 — 조건부 실행

- [ ] E2E 또는 최고비용 검증의 실행 조건과 선택 근거
- [ ] 스킵하는 변경 범위와 스킵이 안전한 이유
- [ ] 조건에 걸려 검증이 실행된 PR의 Actions 로그
- [ ] 조건에 걸리지 않아 검증이 스킵된 PR의 Actions 로그
- [ ] required check와 조건부 실행이 충돌하지 않아 머지 가능한 상태라는 확인
- [ ] 선택한 flaky 대응 정책과 이유 1~2줄

### 3단계 완료조건 — 예산 게이트와 결과 가시성

- [ ] 번들 임계값과 7주차 측정값을 인용한 근거
- [ ] 현재 값의 범위와 여유폭을 정한 이유
- [ ] 필수 환경 변수 검증 코드와 실패 조건
- [ ] 어떤 게이트를 required로 두고 어떤 게이트를 제외했는지에 대한 판단
- [ ] 예산을 일부러 초과한 PR의 빨간불
- [ ] PR 화면에서 무엇이 얼마나 초과됐는지 보이는 코멘트 또는 job summary
- [ ] 수정 후 다시 초록불이 된 증거
- [ ] 실험 PR을 머지하지 않고 실험 변경을 복구했다는 확인

### 4단계 완료조건 — AI 코드리뷰

- [ ] 10주간 합의한 팀 컨벤션이 실제 리뷰 프롬프트에 명문화되어 있음
- [ ] 잘 잡아낸 리뷰 1개와 작성자가 채택한 근거
- [ ] 헛소리한 리뷰 1개와 작성자가 기각한 근거
- [ ] 오탐을 줄이기 위해 수정한 프롬프트의 전후와 변경 이유
- [ ] AI 리뷰를 CI에 붙였다면 안전장치와 required/advisory 배치 판단

AI 리뷰를 CI에 붙이지 않았다면 마지막 항목은 해당 없음으로 기록할 수 있다. 로컬 AI 도구로 리뷰해도
이 단계의 필수 범위를 충족한다.

### 5단계 완료조건 — 결정적 룰 승격

- [ ] AI 또는 사람의 반복 지적 출처
- [ ] 반복 지적 하나를 결정적 게이트로 구현한 코드
- [ ] 위반 코드가 빨간불로 막히는 자가 검증 기록
- [ ] 정상 코드가 통과하는 자가 검증 기록
- [ ] 오탐을 발견했다면 규칙을 좁힌 과정과 재검증 기록
- [ ] 무엇을 AI·사람에게 남기고 무엇을 기계에 둘지 설명한 판단 1문단

### 6단계 완료조건 — 기술 회고

- [ ] `docs/rfc/week10-retrospective.md`가 존재함
- [ ] 기술 결정 2~3개의 문제, 선택, 작성자의 결정, 확인 결과와 한계가 드러남
- [ ] 7·8·9·10주차의 관련 근거를 숫자, URL, 로그와 CI run으로 연결함
- [ ] AI·사람·기계의 역할 구분이 10주 전체에 대한 작성자의 결론으로 이어짐
- [ ] 면접이나 코드 리뷰에서 “왜 그렇게 했는가”를 증거로 설명할 수 있음

### 제출 질문 완료조건

- [ ] E2E를 모든 PR에 required로 둘 때의 문제와 적절한 배치를 2~4문장으로 작성함
- [ ] Lighthouse 점수 하락을 항상 merge blocker로 둘지 2~4문장으로 작성함
- [ ] Preview가 production API를 바라볼 때의 위험과 방어를 2~4문장으로 작성함
- [ ] AI가 만든 workflow를 그대로 머지할 때의 위험과 검증 방법을 2~4문장으로 작성함
- [ ] 네 답 모두 AI가 대신 결론내리지 않고 작성자가 판단과 근거를 직접 작성함

### 최종 완료조건

- [ ] 아래 최종 제출물 표의 모든 필수 산출물이 실제 위치에 존재함
- [ ] 각 완료 주장에 확인 가능한 파일, PR, Actions run, 로그 또는 스크린샷이 연결됨
- [ ] `pnpm check`가 최종 코드에서 통과함
- [ ] 필요하다고 설계한 CI가 최종 상태에서 통과함
- [ ] cache miss, 예산 초과, 규칙 위반을 위해 만든 실험 변경이 남아 있지 않음
- [ ] secret 값이 코드, 문서, 로그와 PR에 남아 있지 않음
- [ ] 선택 과제를 하지 않았다는 이유로 필수 항목을 완료 처리하지 않음
- [ ] 구현과 문서가 어긋나지 않는지 커밋 전에 대조함

## 최종 제출물 위치

| 제출물                                       | 위치                                             |
| -------------------------------------------- | ------------------------------------------------ |
| CI workflow                                  | `.github/workflows/**`                           |
| CI Before/After 측정 기록과 캐시 로그        | PR 본문 또는 `docs/rfc/week10-ci.md`             |
| 조건부 실행 근거와 두 PR 로그                | PR 본문 또는 문서                                |
| 번들 예산 설정과 근거                        | `.size-limit.*` 등과 문서                        |
| 환경 변수 검증 코드                          | `scripts/validate-env.*` 또는 프로젝트 기준 위치 |
| 예산 초과·복구 증거와 PR 실패 리포트         | 스크린샷과 PR 또는 문서                          |
| AI 리뷰 기준, 성공·오탐 사례와 프롬프트 개선 | PR 본문 또는 문서                                |
| 승격한 룰과 자가 검증 기록                   | ESLint/Danger 설정과 문서                        |
| 질문 4개 답변                                | PR 본문 또는 문서                                |
| 10주 기술 회고                               | `docs/rfc/week10-retrospective.md`               |

`docs/rfc/week10-ci.md`는 과제에서 허용한 지정 위치 중 하나다. 모든 산출물을 반드시 이 파일
하나에 넣으라는 뜻은 아니며, PR 본문을 선택했다면 해당 근거를 PR에서 확인할 수 있어야 한다.

## 에이전트 작업 전후 확인

### 작업 전

- [ ] `docs/assignments/week-10.md`와 이 체크리스트를 읽었는가
- [ ] 이번 요청이 필수, 선택 또는 과제 밖 중 어디에 해당하는지 구분했는가
- [ ] 작성자가 직접 정해야 하는 판단을 대신 확정하지 않았는가
- [ ] 현재 증거와 아직 확인하지 않은 항목을 구분했는가

### 작업 후

- [ ] 과제 요구보다 범위를 넓히지 않았는가
- [ ] 계획 문서와 실제 workflow·코드가 일치하는지 대조했는가
- [ ] 실행하지 않은 검증을 통과했다고 기록하지 않았는가
- [ ] 실험용 변경과 secret이 남지 않았는가
- [ ] 새 증거가 생긴 항목만 체크하고 근거 위치를 함께 기록했는가
- [ ] 질의응답에서 새로 확인한 사실·사용자 판단·계획 변경을 `action-plan.html`에 이력으로 추가할
      필요가 있는지 확인했는가
